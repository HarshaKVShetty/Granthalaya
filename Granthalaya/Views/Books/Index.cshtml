@model IEnumerable<Granthalaya.Models.Book>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ttile)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Author)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Category)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumberOfBooks)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AvailableBooks)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ttile)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Author)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Category)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumberOfBooks)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AvailableBooks)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Isbn }) |
            @Html.ActionLink("Details", "Details", new { id = item.Isbn }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Isbn })|
            @Html.ActionLink("Borrow", "Create", "Borrows",new { id = item.Isbn },null)
        </td>
    </tr>
}

</table>
